<table border="3">
    <tr>
        <td>id</td>
        <td>name</td>
        <td>description</td>
        <td>validationdate</td>
    </tr>

    {% for m in tabmarket %}
    <tr>
        <td>{{m.id}}</td>
        <td>{{m.name}}</td>
        <td>{{m.description}}
        <td>{{m.validationdate | date}}</td>
        <td><a href="{{path('app_updatemarket' , {id:m.id} )}}">update</a></td>
    </tr>
    {%endfor%}
</table>